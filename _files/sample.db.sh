# Note: don't use the "-ud" argument here, it will conflict with systemd

# Note: the "runserver.sh" script already defines "-n" and "-x" arguments, so
# don't use them here
DB_ARGS=""

DB_FILE="MYDB.db"
DB_NAME="MYDB"
DB_PORT="2638"
DB_TLOG="MYDB.log"
